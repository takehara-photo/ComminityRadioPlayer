<Window x:Class="ComminityRadioPlayer.GetStations"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComminityRadioPlayer"
        mc:Ignorable="d"
        Title="放送局一覧取得中" Height="100" Width="200" ResizeMode="NoResize">
    <Grid Margin="5,5,5,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Label x:Name="LabelNow"/>
       
            <Label Content="/"/>
            <Label x:Name="LabelCountAll" />
        </StackPanel>
        <ProgressBar Grid.Row="1" Grid.Column="0" x:Name="ProgressBar1"/>
    </Grid>
</Window>
